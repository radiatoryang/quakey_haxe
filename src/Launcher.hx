package ;

import sys.io.Process;

class Launcher {

    public static var currentProcess:Process;

    public static function launch(quakeExePath:String, mapID:String) {
        // var args = new Array<String>();
        // args.push('-basedir "' + Main.BASE_DIR + Main.INSTALLS_PATH + '/' + mapID + '/"');
        // var mapData = Database.instance.db[mapID];

        // currentProcess = new Process(quakeExePath, args, false);

    }
}